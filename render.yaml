services:
  - type: web
    name: qr-autofill-service
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: FLASK_DEBUG
        value: false
      - key: PORT
        value: 5000
    healthCheckPath: /api/qr/health

